import { LightTheme } from "./themes/LightTheme"
import { DarkTheme } from "./themes/DarkTheme"

export const filterTheme = (themeName) => themeObject[themeName]

export const themeObject = {
	LightTheme,
	DarkTheme
}